<h1>this is the campgrounds webpage </h1>

<% campgrounds.forEach(function(campground){   %>
    <div>
        <h4>
            <%= campground["name"] %>
        </h4>
        <img url="<%= campground.image %> ">
    </div>

    <% }) %>